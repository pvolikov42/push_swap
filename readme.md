# push_swap project

The goal is to sort the numbers in stack a in ascending order.

Version
a - 2way sip
b - 2way sip splitted into sequential chunks
c - nway sip recursively splitted into chunks until optimum size
d - c + 1 step ahead optimization
e - d + detecting and skipping already sorted chunktails
